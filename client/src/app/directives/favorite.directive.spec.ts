import { FavoriteDirective } from './favorite.directive';

describe('FavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
