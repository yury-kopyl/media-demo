import {IsAuthorizedDirective} from './isAuthorized.directive';

describe('IsAuthorizedDirective', () => {
    it('should create an instance', () => {
        const directive = new IsAuthorizedDirective();
        expect(directive).toBeTruthy();
    });
});
