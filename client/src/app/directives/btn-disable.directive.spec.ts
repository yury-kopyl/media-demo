import { BtnDisableDirective } from './btn-disable.directive';

describe('BtnDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnDisableDirective();
    expect(directive).toBeTruthy();
  });
});
